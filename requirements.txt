pdfplumber>=0.10.0
openpyxl>=3.1.0
numpy>=1.24.0

# --- スキャンPDF向けOCR（フォールバック順） ---

# [推奨] PaddleOCR（主系 OCR エンジン）
# paddleocr>=2.7.0
# paddlepaddle>=2.6.0   # GPU環境: paddlepaddle-gpu

# [フォールバック] Tesseract（PaddleOCR 未インストール時に使用）
# pytesseract>=0.3.10
# + Tesseract-OCR 本体と jpn.traineddata のインストールが別途必要

# PDF → 画像変換（PaddleOCR / Tesseract 共用）
# pdf2image>=1.17.0
# pillow>=10.0.0
# + Poppler のインストールが別途必要
